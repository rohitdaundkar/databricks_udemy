# This is a Declarative Automation Bundle definition for dab_tutorial.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab_tutorial
  uuid: a44984b4-69b7-4817-b3a6-588a8304abfc

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
# variables:
#   catalog:
#     description: The catalog to use
#   schema:
#     description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: dbc-eef5d97b-f26b.cloud.databricks.com
    # variables:
    #   catalog: workspace
    #   schema: ${workspace.current_user.short_name}
    presets:
      name_prefix: ${workspace.current_user.short_name}
      source_linked_deployment: false
      
  prod:
    mode: production
    workspace:
      host: dbc-eef5d97b-f26b.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/daundkar.rohit@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/prod/.bundle/${bundle.name}/${bundle.target}
    # variables:
    #   catalog: workspace
    #   schema: prod
    permissions:
      - user_name: daundkar.rohit@gmail.com
        level: CAN_MANAGE
